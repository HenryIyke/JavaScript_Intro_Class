const rad = parseInt(prompt("Enter radius"));
const pi = 3.14;

const a = (pi * rad * rad);
const circ = (2 * pi * rad);

alert("The area of the circle is " + a + " and The circumference is " + circ);