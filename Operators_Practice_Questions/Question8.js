const a = parseInt(prompt("Enter side A"));
const b = parseInt(prompt("Enter side B"));
const c = parseInt(prompt("Enter side C"));

const per = a + b + c;

alert("The perimeter of the triangle is " + per);