const length = parseInt(prompt("Enter a length"));
const width = parseInt(prompt("Enter a width"));

per = (2 * ( length + width));
area = (length * width);

alert("The area of the rectangle is " + area + " and The perimeter is " + per);
